/**
 * Complete the function that will return the string in an Array
 * For instance i.e if 
 * Stanley is passed to the function result is be [Stanley]
 * 
 * Also if "I am him" is passed to the function the result will be [i,am,him]
 * @param {*} text 
 */
function ReturnTextInArray(text) {

}

/**
 * Complete the function that will return the array as a string
 * For instance i.e if 
 * [Stanley] is passed to the function result is be "Stanley"
 * 
 * Also if [i,am,him] is passed to the function the result will be "I-am-him"
 * @param {*} array
 */
function ReturnArrayAsString(array) {

}

/**
 * Complete the function that will keys in object attach a pre string
 * For instance i.e if 
 * {"name":"Stanley", "age":"17"} is passed to the function result is be ["key-name","key-age"]
 * 
 * @param {*} obj
 */
function ReturnKeyInObjectAsArrayOfString(obj) {

}

/**
 * Complete the function accept an object, 
 * first get a copy of that object
 * then delete the key name if it exist and return the the initial param intact without modification
 * For instance i.e if 
 * {"name":"Stanley", "age":"17"} is passed to the function after deleting name from the copy of the obj the result should be {"name":"Stanley", "age":"17"}
 * 
 * @param {*} obj
 */


function MutableObject(obj) {

}

module.exports = {
    ReturnTextInArray,
    ReturnArrayAsString,
    ReturnKeyInObjectAsArrayOfString,
    MutableObject
}